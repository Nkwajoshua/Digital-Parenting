{
  "name": "digital-parenting-functions",
  "version": "1.0.0",
  "private": true,
  "main": "entrypoint.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "lint": "echo \"No lint configured\"",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "check": "node --check index.js && node --check callables.js && node --check entrypoint.js"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^5.1.1"
  }
}
