<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions"/>
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.KILL_BACKGROUND_PROCESSES"/>

    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:enableOnBackInvokedCallback="true"
        android:fullBackupContent="false"
        android:label="Digital Parenting"
        android:theme="@style/Theme.DigitalParenting">

        <!-- Child bootstrap: authenticate first, then route to pairing or home. -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Child App Activities -->
        <activity android:name=".ui.WelcomeActivity"
            android:exported="false" />
        <activity android:name=".ui.PairingCodeActivity"
            android:exported="false" />
        <activity android:name=".ui.PermissionsSetupActivity"
            android:exported="false" />
        <activity android:name=".ui.HomeStatusActivity"
            android:exported="false" />
        <activity android:name=".ui.DiagnosticsActivity"
            android:exported="false" />
        <activity android:name=".ui.ActivityAlertsActivity"
            android:exported="false" />
        <activity android:name=".ui.BlockedActivity"
            android:exported="false" />
        <activity android:name=".ui.RequestMoreTimeActivity"
            android:exported="false" />

        <!--
            Persistent Child parental-control orchestration does not fit a standard
            foreground-service category. Android reviews this free-form subtype when
            specialUse is declared for distribution through Google Play.
        -->
        <service
            android:name=".service.MonitoringService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="continuous_child_device_parental_control_monitoring_and_app_limit_enforcement" />
        </service>

        <!-- Firebase Cloud Messaging receiver for Child-directed updates and token rotation. -->
        <service
            android:name=".service.ChildFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Boot Receiver -->
        <receiver android:name=".service.BootReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
            </intent-filter>
        </receiver>

        <!-- Child Accessibility Service -->
        <service
            android:name=".service.ChildAccessibilityService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="false">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_config" />
        </service>

    </application>
</manifest>
