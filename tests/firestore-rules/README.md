# Firestore Rules Test Scaffold

This folder is a placeholder for future emulator-backed automated Firestore rules tests.

## Intended future setup
- Add Firebase emulator test runner (Node or JavaScript SDK test harness).
- Seed parent/child fixture docs.
- Assert ALLOW/DENY behavior for the scenarios listed in `FIRESTORE_RULES_TEST_PLAN.md`.

## Manual fallback today
Use `FIRESTORE_RULES_TEST_PLAN.md` for role-based manual tests in Emulator Suite.
